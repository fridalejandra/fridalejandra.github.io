<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Head content here -->
</head>
<body>
    <h1>{{ page.title }}</h1>
    {% for course in site.data.courses %}
    <h2>{{ course.name }}</h2>
    <ul>
        {% for offering in course.offerings %}
        <li>{{ offering.semester }}, {{ offering.location }}</li>
        {% endfor %}
    </ul>
    {% endfor %}
</body>
</html>
